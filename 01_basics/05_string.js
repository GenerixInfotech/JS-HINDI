const name = "Manish"
const repoCount = 30

console.log(name + repoCount + " value");

console.log(`My name is ${name} and my total repogotry is ${repoCount}`);

const gameName = new String('GameZone')